type: object
required:
  - value
  - ns
properties:
  id:
    type: string
    format: uuid
    readOnly: true
    description: Present when reached via /oa/publicationRequest; absent via /oa/titleInstances
  value:
    type: string
  ns:
    type: object
    required:
      - value
    properties:
      id:
        type: string
        format: uuid
        readOnly: true
        description: Present when reached via /oa/publicationRequest; absent via /oa/titleInstances
      value:
        type: string
