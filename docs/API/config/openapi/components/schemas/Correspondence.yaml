type: object
required:
  - id
  - dateOfCorrespondence
  - content
  - correspondent
  - status
  - mode
properties:
  id:
    type: string
    format: uuid
    readOnly: true
  owner:
    type: object
    required:
      - id
    properties:
      id:
        type: string
        format: uuid
  lastUpdated:
    type: string
    format: date-time
    readOnly: true
  dateOfCorrespondence:
    type: string
    format: date
  content:
    type: string
  correspondent:
    type: string
  status:
    oneOf:
      - type: string
      - $ref: ../../common/schemas/Refdata.yaml
  mode:
    oneOf:
      - type: string
      - $ref: ../../common/schemas/Refdata.yaml
  category:
    oneOf:
      - type: string
      - $ref: ../../common/schemas/Refdata.yaml
