import mod_oa.kb.IdentifierOccurrence

import groovy.transform.*

@Field
IdentifierOccurrence identifierOccurrence
json g.render(identifierOccurrence, [expand: ['identifier', 'status'], excludes: ['id']])
